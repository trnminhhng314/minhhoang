/*###Begin banned keyword - each of the following line if appear in code will raise error. regex supported
###End banned keyword*/

#include <iostream>
using namespace std;

int GetLuckyOne(int, int, int);

int main() {
    cin.tie(NULL);
    std::ios_base::sync_with_stdio(false);
    int t, n, m, s;
    cin >> t; // Nhập số test case
    // Với mỗi test case, người dùng nhập 3 giá trị n, m, s
    for (int i = 0; i < t; i++){
        cin >> n >> m >> s;
        cout << GetLuckyOne(n, m, s) << "\n";
    }
    return 0;
}

abcdefghiasdas;ldkasdas
asdlaksglf';askd'alsd
fal;sdjkawpodkjapsod
afo'lawkdal'w;dka'sw
wfl;awkjd;olaskda[pw'd
